# Environment Configuration Template
# Copy this file to .env.dev and update with your values
# This file contains NON-SENSITIVE configuration only
# Sensitive data (API keys, passwords, tokens) should be stored in AWS Secrets Manager

# AWS Configuration
#AWS_PROFILE=example
AWS_REGION=eu-north-1
AWS_ACCOUNT=123456789012

# Secrets Manager Configuration (secret IDs only, not the actual secrets)
# Store sensitive data like API keys, passwords, tokens in AWS Secrets Manager
SECRETS_ID=my-app-secrets

# Optional: External Authorizer Configuration
# AUTHORIZER_ARN=arn:aws:lambda:eu-north-1:123456789012:function:your-existing-authorizer

# =============================================================================
# SENSITIVE DATA - DO NOT PUT IN .env FILES!
# =============================================================================
# Store these in AWS Secrets Manager using the secret IDs above:
# - Database passwords
# - API keys and tokens
# - Private keys
# - Webhook secrets
# - Any other sensitive configuration
